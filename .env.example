# PassPass - Root Environment Variables Example
# Duplicate this file as .env for local development

# PostgreSQL Configuration
POSTGRES_USER=passpass
POSTGRES_PASSWORD=passpass_dev_secret
POSTGRES_DB=passpass_db
# If port 5432 is already used by another service, change POSTGRES_PORT (e.g. 5433)
POSTGRES_PORT=5432
